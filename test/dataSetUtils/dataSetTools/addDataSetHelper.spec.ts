

describe("add dataSet helper methods test suite", function () {
	// const content = fs.readFileSync("./test/resources/archives/courses_demo.zip").toString("base64");
	// it("addDataSetHelper method tester", async function () {
	// 	await addDatasetHelper(content, "courses", InsightDatasetKind.Courses);
	// });
});
